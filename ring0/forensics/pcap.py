# Auto search with regex for flag or b64 encoded flag
# Pretty dump pcap with tcpick -C -yP -r tcp_dump.pcap
# Dump pcap with tcpdump -qns 0 -A -r blah.pcap
# Auto dump all files, search for string
# Dump strings in increasing length order
# Filter for FLAG after base64 decoding, hex decoding, etc
# Can tcpdump with various filters

