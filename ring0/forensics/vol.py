# Automatically detects and selects profile
# Can list and dump processes
# Can list and dump files
# Can hashdump
# Each time, checks for flag with regular expression, also b64 version
# Automatic strings with regex of img
# Brute force all of these things
# List command line commands
